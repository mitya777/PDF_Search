<%= form_for @pdf, :html => {:multipart => true} do |f| %>
  <% if @pdf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdf.errors.count, "error") %> prohibited this pdf from being saved:</h2>

      <ul>
      <% @pdf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="filed">
    <%= f.label :file %><br />
    <%= f.file_field :file %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
